# Dependency management is handled by uv via pyproject.toml.
# This file is kept only for reference — do NOT use pip install -r requirements.txt.
#
# Setup:
#   uv sync                  # installs all deps into .venv (Python 3.12)
#   uv sync --group build    # also installs build tools (cython)
